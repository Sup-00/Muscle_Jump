using UnityEngine;

public class JumpTrigger : MonoBehaviour
{
    /*private void OnTriggerEnter(Collider other)
    {
        if (other.GetComponent<Enemy>() || other.GetComponent<EnemyIgnore>())
        {
            Destroy(other.gameObject);
        }
    }*/
}